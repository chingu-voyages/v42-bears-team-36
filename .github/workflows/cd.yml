# name: CD

# on:
#   push:
#     branches:
#       - main
# jobs:
#   Deploy:
#     runs-on: ubuntu-latest

#     steps:
#       # Check out the code
#       - uses: actions/checkout@v2

#       # Set up Node.js
#       - uses: actions/setup-node@v2
#         with:
#           node-version: 18.x

#       # Install dependencies
#       - run: npm install

#       - run: npm run build
      
#       # test
#       - run: npm test

#       # deploy
#       - name: Deploy next app in vercel
#         uses: amondnet/vercel-action@v20
#         with:
#           vercel-token: ${{ secrets.VERCEL_TOKEN }}
#           vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
#           vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
#           vercel-args: --prod

# Waiting permission for vercel by chingu-voyages
